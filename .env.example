# Optional: set these for Supabase cloud sync. Never commit service-role/secret/JWT keys.
# Vercel reads these during `npm run build` and writes public/config.js.
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_public_key_here
